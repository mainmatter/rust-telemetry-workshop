[package]
name = "what_to_log"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
log = { workspace = true, features = ["std"] }

[dev-dependencies]
helpers = { workspace = true }
